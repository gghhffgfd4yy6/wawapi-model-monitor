// push_config.local.js 示例模板
// 用法：复制本文件为 push_config.local.js 并填入你的密钥
//   cp push_config.local.js.example push_config.local.js
// ⚠️ push_config.local.js 已被 .gitignore 忽略（含真实密钥，绝不提交！）；本示例文件可安全入库
// 字段说明见 xbk_sendNotify_slim.js 顶部 push_config；未配置的通道保持注释即可
module.exports = {
    // Push+（https://www.pushplus.plus/）
    // PUSH_PLUS_TOKEN: '你的token',
    // PUSH_PLUS_USER: '',

    // Server酱 Turbo（https://sct.ftqq.com/）
    // PUSH_KEY: 'SCTxxxxxxxxxxxxxxxxxxxxxxxx',

    // Bark（iOS 推送，https://github.com/Finb/Bark）
    // BARK_PUSH: 'https://api.day.app/你的设备码',   // 多设备用 # 分隔
    // Bark 扩展参数（可选）：归档/分组/声音/图标/时效性/跳转URL
    // BARK_ARCHIVE: '', '1' 存档
    // BARK_GROUP: '',  '分组名'
    // BARK_SOUND: '',  '声音名'
    // BARK_ICON: '',   '图标URL'
    // BARK_LEVEL: '',  'active|timeSensitive|passive'
    // BARK_URL: '',    '点击跳转URL'

    // PushMe（https://pushme.xyz/）
    // PUSHME_KEY: '你的key',
    // PUSHME_URL: 'https://push.i-i.me', // 自定义服务器（可选）

    // 企业微信机器人 webhook
    // QYWX_KEY: '你的webhook地址最后一段key',

    // wxpusher（https://wxpusher.zjiecode.com/）
    // WX_pusher_appToken: '你的appToken',
    // WX_pusher_topicIds: '主题ID(多个用逗号分隔)',
    // 多应用自动分流（每条消息只发一个应用；两边订阅者应保持一致）：
    // WX_pusher_channels: [
    //     { appToken: '应用1appToken', topicIds: '主题ID1' },
    //     { appToken: '应用2appToken', topicIds: '主题ID2' },
    // ],

    // 息知（https://xz.qqoq.net/）
    // WX_XIZHI_KEY: 'https://xizhi.qqoq.net/你的key',

    // PushDeer（https://www.pushdeer.com/）
    // DEER_KEY: '你的key',
    // DEER_URL: 'https://api2.pushdeer.com/message/push', // 自定义服务器（可选）

    // Telegram Bot（token + chat_id 需齐备才生效）
    // TG_BOT_TOKEN: '123456:ABC-DEF...',
    // TG_USER_ID: '你的chat_id',
    // TG_API_HOST: '', // 自定义 API host（可选，默认 api.telegram.org）
    // TG_PROXY_HOST/TG_PROXY_PORT: 保留配置项（当前项目未接入 HTTP 代理，配置不生效；需要代理请用 TG_API_HOST）

    // 一言：推送内容尾部附加随机一句话（'false' 关闭）
    // HITOKOTO: 'true',
};
